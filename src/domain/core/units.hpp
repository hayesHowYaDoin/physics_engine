#ifndef DOMAIN_CORE_UNITS_HPP
#define DOMAIN_CORE_UNITS_HPP

namespace domain::units
{

    using Degrees = float;

    using Radians = float;

    using Meters = float;

    using MetersPerSecond = float;

    using MetersPerSecondSquared = float;

    using Kilograms = float;

    using Newtons = float;

} // namespace domain

#endif // DOMAIN_CORE_UNITS_HPP
